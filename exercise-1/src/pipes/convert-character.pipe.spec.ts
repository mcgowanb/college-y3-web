import { ConvertCharacterPipe } from './convert-character.pipe';

describe('ConvertCharacterPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertCharacterPipe();
    expect(pipe).toBeTruthy();
  });
});
